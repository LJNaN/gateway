name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Add server to known_hosts
        run: ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Ensure target directory exists
        run: ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "mkdir -p /app/gateway"

      # 证书不进仓库（私钥不能提交），只能手工 scp 到服务器。这里先拦一道：
      # 证书缺失时 nginx 会直接启动失败，而网关一挂连 80 端口的 HTTP 也没了，
      # 所以宁可这次部署中止，也不要让容器起不来。
      - name: Preflight - 检查服务器上有 TLS 证书
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "ls /app/gateway/certs/*.pem /app/gateway/certs/*.key" \
            || { echo "::error::/app/gateway/certs/ 缺少 .pem/.key，已中止部署。请先 scp 证书上去（见 README「HTTPS 证书」）"; exit 1; }

      - name: Sync files via rsync
        run: |
          # --exclude='certs' 有两个作用：不上传本地的 certs/，以及让 --delete
          # 不动服务器上的 certs/（rsync 默认不删除被 exclude 的文件）。
          rsync -avz --delete --exclude='.git' --exclude='.github' --exclude='certs' \
            -e 'ssh -o ServerAliveInterval=30' \
            ./ ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/app/gateway/

      - name: Docker compose deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /app/gateway
            docker network create web 2>/dev/null || true
            docker compose up -d
